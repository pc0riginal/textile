version: '3.8'

services:
  app:
    build: .
    container_name: textile_app
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      MONGODB_URL: mongodb+srv://bapspalanapurmandir_db_user:JZk14IqQigujWwCi@cluster0.qf8igco.mongodb.net/
      DATABASE_NAME: textile_erp
      SECRET_KEY: your-super-secret-key-change-this-in-production
      ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 30
    # No local dependencies - using MongoDB Atlas
    volumes:
      - ./app/static/uploads:/app/app/static/uploads
    networks:
      - textile_network

networks:
  textile_network:
    driver: bridge